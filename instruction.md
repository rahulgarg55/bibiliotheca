The Bibliotheca library management backend is running at `http://localhost:5000`. The source code is at `/app`. The backend is a Node.js/Express API backed by PostgreSQL, Redis, and Kafka. The database has been seeded and an admin account exists with email `admin@bibliotheca.com` and password `Admin@1234`.

Several API endpoints are broken or missing. Fix them so that the full test suite passes. The admin-only routes must return 403 for regular users and 401 for unauthenticated requests. Book borrowing must enforce availability and prevent duplicate active loans for the same user. Returning a book must calculate and record overdue fines at $0.50 per day. Reviews must aggregate the average rating on the book record after each submission. Restart the backend service after making code changes.
